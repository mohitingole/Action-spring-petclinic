---
name: "spring-petclinic"

on:
    push:
        branches:
           - main

jobs:
    build:  
       runs-on: "ubuntu-24.04"       
       steps:
          - uses: actions/checkout@v4
          - name: mvn package
            run: mvn package
